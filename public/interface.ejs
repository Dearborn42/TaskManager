<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="../styles.css">
</head>
<body>
    <button onclick="createTask()" class="create">Create Task</button>
    <br>
    <div class="main">
        <% tasks.forEach((task)=> { %>
            <div class="container result <%=task.id%>">
                <div>
                    <input type="checkbox" id="checkbox" class="<%= task.id %>">
                    <span class="<%=task.id%>"><%= task.name %></span>
                </div>
                <span class="<%=task.id%>">
                    <%=task.description%>
                </span>
                <div>
                    <button onclick="editTaskName('<%= task.id %>')">Edit Name</button>
                    <button onclick="editTaskDesc('<%= task.id %>')">Edit Desc</button>
                    <button onclick="deleteTask('<%= task.id %>')">Delete</button>
                </div>
            </div>
        <% }); %>
    </div>
    <script src="../check.js"></script>
</body>
</html>